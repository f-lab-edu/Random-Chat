spring:
  jwt:
    secret: "asdfdasfadsvmadsvmasdlfmasdfadsmfdaskfasdfasd"
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: root
    password: root
  h2:
    console:
      enabled: true
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

#  test:
#    database:
#      replace: none   # 이 설정은 임베디드 데이터베이스 사용을 막습니다.